<%= turbo_frame_tag muscle do %>
  <div class="row py-2 border-top">
    <div class="col-4 my-auto">
      <%= muscle.name %>
    </div>
    <div class="col-4 my-auto">
      <%= muscle.code %>
    </div>
    <div class="col-4">
      <div class="d-flex justify-content-end">
        <%= link_to "編集", edit_muscle_path(muscle), class: "btn btn-sm btn-outline-primary me-2" %>
        <%= button_to "削除", muscle, method: :delete, class: "btn btn-sm btn-outline-danger" %>
      </div>
    </div>
  </div>
<% end %>
